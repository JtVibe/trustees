/** Placeholder for the Cart tab — shows its name until the real screen is built. */

import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function CartScreen() {
  return <PlaceholderScreen name="Cart" />;
}
