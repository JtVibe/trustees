/** Placeholder for the Shop tab — shows its name until the real screen is built. */

import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function ShopScreen() {
  return <PlaceholderScreen name="Shop" />;
}
